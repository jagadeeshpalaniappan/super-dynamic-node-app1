---
declared-services:
  super-mongo:
    label: mongodb
    plan: 100
applications:
- name: super-dynamic-node
  host: super-dynamic-node-${random-word}
  memory: 128M
  services:
    - super-mongo
  env:
    NODE_ENV: cloud-foundry
